# Prompt files

Reusable slash-commands live here. This folder ships `plan.prompt.md` (a `/plan` command that
explores the codebase and writes PLAN.md WITHOUT writing code) and `review.prompt.md` (runs
the payments-reviewer persona from `templates/`).
