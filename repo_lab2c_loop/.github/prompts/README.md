# Prompt files

Reusable slash-commands live here. This folder ships `plan.prompt.md` (a `/plan` command that
explores the codebase and writes PLAN.md WITHOUT writing code), `review.prompt.md` (runs the
payments-reviewer persona from `templates/`), and `loop.prompt.md` (the `/loop` command that
iterates the implementation until the tests pass).
