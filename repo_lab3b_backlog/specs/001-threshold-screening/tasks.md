# Tasks
*(Canonical /speckit.tasks output — feed /speckit.taskstoissues to create GitHub issues.)*
1. Create `data/screening-thresholds.json` (VND 200000000, USD 1000000). [S]
2. Failing tests in `tests/screening.test.ts` covering the 5 acceptance cases. [S]
3. Extend `screen()`: threshold check + fail-closed + precedence. [M]
4. Run full suite; confirm no regression in payments/audit behavior. [S]
